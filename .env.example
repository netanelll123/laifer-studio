# Canonical production URL — used for metadata, OpenGraph, robots and sitemap.
NEXT_PUBLIC_SITE_URL=https://laifer-studio.vercel.app

# Contact form destination. The /api/contact route forwards submissions here.
# Until this is set, the API returns 501 and the form shows a graceful error.
CONTACT_ENDPOINT_URL=
